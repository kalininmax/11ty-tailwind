<!DOCTYPE html>
<html class="page w-full h-full" lang="en">
  <head>
    <meta charset="utf-8"/>
    <title>{% block title %}{% endblock %} | {{ site.name }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>

    {% block stylesheets %}
      <link rel="stylesheet" href="{{ STATIC_PATH }}/css/tailwind.css">
      <link rel="stylesheet" href="{{ STATIC_PATH }}/css/styles.css">
    {% endblock stylesheets %}
  </head>
  <body class="page__body flex flex-col w-full h-full">
    {% svg_sprite %}

    {% include "../components/header/header.njk" %}

    <main class="page__main flex-grow bg-white-1" id="main-content" tabindex="-1">
      {% block content %}{% endblock %}
    </main>

    {% block scripts %}
      <script src="{{ STATIC_PATH }}/js/bundle.js"></script>
    {% endblock scripts %}
  </body>
</html>
